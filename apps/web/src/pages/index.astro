---
import { ThemeToggle } from "../components/ThemeToggle.tsx";
import WaitlistForm from "../components/WaitlistForm.tsx";
import "../styles/global.css";
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- Prevent dark mode flash -->
		<script>
			(function() {
				function getSystemTheme() {
					return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
				}
				
				const storedTheme = localStorage.getItem('theme') || 'system';
				const theme = storedTheme === 'system' ? getSystemTheme() : storedTheme;
				
				if (theme === 'light') {
					document.documentElement.classList.add('light');
				}
				// Dark mode is default, no class needed
			})();
		</script>
		
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono&display=swap" rel="stylesheet">
		<title>Syntaxia</title>
	</head>
	<body class="min-h-screen bg-background text-foreground font-mono">
		<header class="border-b border-terminal-green/30 bg-terminal-dark px-4 md:px-6 font-mono">
			<div class="flex h-16 items-center justify-between">
				<div class="flex items-center gap-1">
					<span class="text-terminal-amber">$</span>
					<span class="font-mono font-bold text-terminal-green">syntaxia</span>
					<span class="text-terminal-green animate-pulse">_</span>
				</div>
									<div class="flex items-center gap-3">
						<ThemeToggle client:idle />
						<button
						class="font-mono text-xs bg-transparent border border-terminal-green/30 text-terminal-green hover:bg-terminal-green/10 hover:text-terminal-amber px-3 py-1 transition-colors h-8 min-w-20"
						onclick="window.location.href = '/'"
					>
						./login
					</button>
				</div>
			</div>
		</header>

		<main class="container mx-auto px-4 py-8 max-w-4xl">
			<!-- Hero Section -->
			<div class="text-center mb-12">
				<h1 class="text-2xl md:text-3xl font-bold text-terminal-green mb-4">$ ./syntaxia --help</h1>
				<p class="text-terminal-green/80 text-lg mb-8 max-w-2xl mx-auto">
					Don't let technical phone screens block great offers
				</p>
			</div>

			<!-- Features Grid -->
			<div class="grid md:grid-cols-2 gap-6 mb-12">
				<div class="border border-terminal-green/30 p-6 bg-card">
					<div class="flex items-center mb-3">
						<span class="text-terminal-amber mr-2">$</span>
						<h3 class="text-lg font-semibold text-terminal-green">real-conversation</h3>
					</div>
					<p class="text-terminal-green/70 text-sm leading-relaxed">
						Stop typing, start talking. Practice the back-and-forth of real phone screens while you problem-solve.
					</p>
				</div>

				<div class="border border-terminal-green/30 p-6 bg-card">
					<div class="flex items-center mb-3">
						<span class="text-terminal-amber mr-2">$</span>
						<h3 class="text-lg font-semibold text-terminal-green">actual-questions</h3>
					</div>
					<p class="text-terminal-green/70 text-sm leading-relaxed">
						Paste the job description, get their exact interview questions. Stop wasting time on random LeetCode.
					</p>
				</div>

				<div class="border border-terminal-green/30 p-6 bg-card">
					<div class="flex items-center mb-3">
						<span class="text-terminal-amber mr-2">$</span>
						<h3 class="text-lg font-semibold text-terminal-green">performance-report</h3>
					</div>
					<p class="text-terminal-green/70 text-sm leading-relaxed">
						Get a brutal breakdown of what went wrong. Know exactly what to fix before the real thing.
					</p>
				</div>

				<div class="border border-terminal-green/30 p-6 bg-card">
					<div class="flex items-center mb-3">
						<span class="text-terminal-amber mr-2">$</span>
						<h3 class="text-lg font-semibold text-terminal-green">blind-spots</h3>
					</div>
					<p class="text-terminal-green/70 text-sm leading-relaxed">
						Track your weak areas clearly. Know when you're ready to schedule with confidence.
					</p>
				</div>
			</div>

			<!-- Waitlist Section -->
			<div class="border border-terminal-amber/50 p-8 bg-card/50 text-center">
				<div class="mb-6">
					<h2 class="text-xl font-bold text-terminal-amber mb-2">$ ./join-waitlist --early-access</h2>
					<p class="text-terminal-green/70">
						Join the waitlist. If selected, you'll get free sessions to test our beta.
					</p>
				</div>

				<WaitlistForm client:visible />
			</div>

			<!-- Footer -->
			<div class="mt-12 text-center">
				<div class="border-t border-terminal-green/20 pt-6">
					<p class="text-terminal-green/50 text-sm">
						Built for developers, by developers. Practice like you're already hired.
					</p>
					<div class="mt-2 text-xs text-terminal-green/30">$ git commit -m "ship syntaxia v1.0"</div>
				</div>
			</div>
		</main>
	</body>
</html>
