{
  "name": "@syntaxia/backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "convex dev --tail-logs",
    "setup": "convex dev --once && convex env set VALIDATE_ENV=true",
    "seed": "convex dev --once && convex run init"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@auth/core": "catalog:",
    "@convex-dev/auth": "^0.0.80",
    "@convex-dev/polar": "^0.6.1",
    "@polar-sh/sdk": "^0.34.9",
    "@t3-oss/env-core": "catalog:",
    "arktype": "catalog:",
    "convex": "^1.25.4",
    "convex-helpers": "^0.1.101"
  }
}
